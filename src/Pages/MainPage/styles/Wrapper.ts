import { Box, styled } from "@mui/material";

const Wrapper = styled(Box)`
  width: 100vw;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: rgb(106, 158, 121);
`;

export default Wrapper;
